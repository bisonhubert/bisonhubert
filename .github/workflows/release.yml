name: Release
run-name: Release
on: 
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  # test:
  #   uses: ./.github/workflows/test.yml

  release:
    # needs: [test]
    uses: ./.github/workflows/deploy.yml

  # smoke_test:

  # rollback
  # if fail()
